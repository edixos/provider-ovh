apiVersion: ovh.edixos.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      name: provider-ovh
      namespace: crossplane-system
      key: credentials
