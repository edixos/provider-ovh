---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: tcpfarms.lb.ovh.m.edixos.io
spec:
  group: lb.ovh.m.edixos.io
  names:
    categories:
    - crossplane
    - managed
    - ovh
    kind: TCPFarm
    listKind: TCPFarmList
    plural: tcpfarms
    singular: tcpfarm
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TCPFarm is the Schema for the TCPFarms API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TCPFarmSpec defines the desired state of TCPFarm
            properties:
              forProvider:
                properties:
                  balance:
                    description: Load balancing algorithm. roundrobin if null (first,
                      leastconn, roundrobin, source)
                    type: string
                  displayName:
                    description: Readable label for loadbalancer farm
                    type: string
                  port:
                    description: Port attached to your farm ([1..49151]). Inherited
                      from frontend if null
                    type: number
                  probe:
                    description: define a backend healthcheck probe
                    items:
                      properties:
                        forceSsl:
                          description: Force use of SSL (TLS)
                          type: boolean
                        interval:
                          description: probe interval, Value between 30 and 3600 seconds,
                            default 30
                          type: number
                        match:
                          description: What to match pattern against (contains, default,
                            internal, matches, status)
                          type: string
                        method:
                          description: HTTP probe method (GET, HEAD, OPTIONS, internal)
                          type: string
                        negate:
                          description: Negate probe result
                          type: boolean
                        pattern:
                          description: Pattern to match against match
                          type: string
                        port:
                          description: Port attached to your farm ([1..49151]). Inherited
                            from frontend if null
                          type: number
                        type:
                          description: 'Valid values : http, internal, mysql, oco,
                            pgsql, smtp, tcp'
                          type: string
                        url:
                          description: URL for HTTP probe type.
                          type: string
                      type: object
                    type: array
                  serviceName:
                    description: The internal name of your IP load balancing
                    type: string
                  stickiness:
                    description: Stickiness type. No stickiness if null (sourceIp)
                    type: string
                  vrackNetworkId:
                    description: Internal Load Balancer identifier of the vRack private
                      network to attach to your farm, mandatory when your Load Balancer
                      is attached to a vRack
                    type: number
                  zone:
                    description: Zone where the farm will be defined (ie. GRA, BHS
                      also supports ALL)
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  balance:
                    description: Load balancing algorithm. roundrobin if null (first,
                      leastconn, roundrobin, source)
                    type: string
                  displayName:
                    description: Readable label for loadbalancer farm
                    type: string
                  port:
                    description: Port attached to your farm ([1..49151]). Inherited
                      from frontend if null
                    type: number
                  probe:
                    description: define a backend healthcheck probe
                    items:
                      properties:
                        forceSsl:
                          description: Force use of SSL (TLS)
                          type: boolean
                        interval:
                          description: probe interval, Value between 30 and 3600 seconds,
                            default 30
                          type: number
                        match:
                          description: What to match pattern against (contains, default,
                            internal, matches, status)
                          type: string
                        method:
                          description: HTTP probe method (GET, HEAD, OPTIONS, internal)
                          type: string
                        negate:
                          description: Negate probe result
                          type: boolean
                        pattern:
                          description: Pattern to match against match
                          type: string
                        port:
                          description: Port attached to your farm ([1..49151]). Inherited
                            from frontend if null
                          type: number
                        type:
                          description: 'Valid values : http, internal, mysql, oco,
                            pgsql, smtp, tcp'
                          type: string
                        url:
                          description: URL for HTTP probe type.
                          type: string
                      type: object
                    type: array
                  serviceName:
                    description: The internal name of your IP load balancing
                    type: string
                  stickiness:
                    description: Stickiness type. No stickiness if null (sourceIp)
                    type: string
                  vrackNetworkId:
                    description: Internal Load Balancer identifier of the vRack private
                      network to attach to your farm, mandatory when your Load Balancer
                      is attached to a vRack
                    type: number
                  zone:
                    description: Zone where the farm will be defined (ie. GRA, BHS
                      also supports ALL)
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.serviceName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceName)
                || (has(self.initProvider) && has(self.initProvider.serviceName))'
            - message: spec.forProvider.zone is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.zone)
                || (has(self.initProvider) && has(self.initProvider.zone))'
          status:
            description: TCPFarmStatus defines the observed state of TCPFarm.
            properties:
              atProvider:
                properties:
                  balance:
                    description: Load balancing algorithm. roundrobin if null (first,
                      leastconn, roundrobin, source)
                    type: string
                  displayName:
                    description: Readable label for loadbalancer farm
                    type: string
                  id:
                    type: string
                  port:
                    description: Port attached to your farm ([1..49151]). Inherited
                      from frontend if null
                    type: number
                  probe:
                    description: define a backend healthcheck probe
                    items:
                      properties:
                        forceSsl:
                          description: Force use of SSL (TLS)
                          type: boolean
                        interval:
                          description: probe interval, Value between 30 and 3600 seconds,
                            default 30
                          type: number
                        match:
                          description: What to match pattern against (contains, default,
                            internal, matches, status)
                          type: string
                        method:
                          description: HTTP probe method (GET, HEAD, OPTIONS, internal)
                          type: string
                        negate:
                          description: Negate probe result
                          type: boolean
                        pattern:
                          description: Pattern to match against match
                          type: string
                        port:
                          description: Port attached to your farm ([1..49151]). Inherited
                            from frontend if null
                          type: number
                        type:
                          description: 'Valid values : http, internal, mysql, oco,
                            pgsql, smtp, tcp'
                          type: string
                        url:
                          description: URL for HTTP probe type.
                          type: string
                      type: object
                    type: array
                  serviceName:
                    description: The internal name of your IP load balancing
                    type: string
                  stickiness:
                    description: Stickiness type. No stickiness if null (sourceIp)
                    type: string
                  vrackNetworkId:
                    description: Internal Load Balancer identifier of the vRack private
                      network to attach to your farm, mandatory when your Load Balancer
                      is attached to a vRack
                    type: number
                  zone:
                    description: Zone where the farm will be defined (ie. GRA, BHS
                      also supports ALL)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
