---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projectranchers.cloud.ovh.edixos.io
spec:
  group: cloud.ovh.edixos.io
  names:
    categories:
    - crossplane
    - managed
    - ovh
    kind: ProjectRancher
    listKind: ProjectRancherList
    plural: projectranchers
    singular: projectrancher
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectRancher is the Schema for the ProjectRanchers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectRancherSpec defines the desired state of ProjectRancher
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  projectId:
                    description: |-
                      (String) Project ID
                      Project ID
                    type: string
                  rancherId:
                    description: |-
                      (String) Unique identifier
                      Rancher ID
                    type: string
                  targetSpec:
                    description: (Attributes) Target specification for the managed
                      Rancher service (see below for nested schema)
                    properties:
                      iamAuthEnabled:
                        description: |-
                          (Bool) Allows Rancher to use identities managed by OVHcloud IAM (Identity and Access Management) to control access
                          Allows Rancher to use identities managed by OVHcloud IAM (Identity and Access Management) to control access
                        type: boolean
                      ipRestrictions:
                        description: (Attributes List) List of allowed CIDR blocks
                          for a managed Rancher service's IP restrictions. When empty,
                          any IP is allowed (see below for nested schema)
                        items:
                          properties:
                            cidrBlock:
                              description: |-
                                (String) Allowed CIDR block (/subnet is optional, if unspecified then /32 will be used)
                                Allowed CIDR block (/subnet is optional, if unspecified then /32 will be used)
                              type: string
                            description:
                              description: |-
                                (String) Description of the allowed CIDR block
                                Description of the allowed CIDR block
                              type: string
                          type: object
                        type: array
                      name:
                        description: |-
                          (String) Name of the managed Rancher service
                          Name of the managed Rancher service
                        type: string
                      plan:
                        description: |-
                          (String) Plan of the managed Rancher service. Available plans for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/plan
                          Plan of the managed Rancher service. Available plans for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/plan
                        type: string
                      version:
                        description: |-
                          (String) Version of the managed Rancher service. Available versions for an existing managed Rancher can be retrieved using ovh_cloud_project_rancher_version datasource. Default is the latest version.
                          Version of the managed Rancher service. Available versions for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/version. Default is the latest version.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  projectId:
                    description: |-
                      (String) Project ID
                      Project ID
                    type: string
                  rancherId:
                    description: |-
                      (String) Unique identifier
                      Rancher ID
                    type: string
                  targetSpec:
                    description: (Attributes) Target specification for the managed
                      Rancher service (see below for nested schema)
                    properties:
                      iamAuthEnabled:
                        description: |-
                          (Bool) Allows Rancher to use identities managed by OVHcloud IAM (Identity and Access Management) to control access
                          Allows Rancher to use identities managed by OVHcloud IAM (Identity and Access Management) to control access
                        type: boolean
                      ipRestrictions:
                        description: (Attributes List) List of allowed CIDR blocks
                          for a managed Rancher service's IP restrictions. When empty,
                          any IP is allowed (see below for nested schema)
                        items:
                          properties:
                            cidrBlock:
                              description: |-
                                (String) Allowed CIDR block (/subnet is optional, if unspecified then /32 will be used)
                                Allowed CIDR block (/subnet is optional, if unspecified then /32 will be used)
                              type: string
                            description:
                              description: |-
                                (String) Description of the allowed CIDR block
                                Description of the allowed CIDR block
                              type: string
                          type: object
                        type: array
                      name:
                        description: |-
                          (String) Name of the managed Rancher service
                          Name of the managed Rancher service
                        type: string
                      plan:
                        description: |-
                          (String) Plan of the managed Rancher service. Available plans for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/plan
                          Plan of the managed Rancher service. Available plans for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/plan
                        type: string
                      version:
                        description: |-
                          (String) Version of the managed Rancher service. Available versions for an existing managed Rancher can be retrieved using ovh_cloud_project_rancher_version datasource. Default is the latest version.
                          Version of the managed Rancher service. Available versions for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/version. Default is the latest version.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.projectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.projectId)
                || (has(self.initProvider) && has(self.initProvider.projectId))'
            - message: spec.forProvider.targetSpec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.targetSpec)
                || (has(self.initProvider) && has(self.initProvider.targetSpec))'
          status:
            description: ProjectRancherStatus defines the observed state of ProjectRancher.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: |-
                      (String) Date of the managed Rancher service creation
                      Date of the managed Rancher service creation
                    type: string
                  currentState:
                    description: (Attributes) Current configuration applied to the
                      managed Rancher service (see below for nested schema)
                    properties:
                      iamAuthEnabled:
                        description: |-
                          (Bool) Allows Rancher to use identities managed by OVHcloud IAM (Identity and Access Management) to control access
                          Allows Rancher to use identities managed by OVHcloud IAM (Identity and Access Management) to control access
                        type: boolean
                      ipRestrictions:
                        description: (Attributes List) List of allowed CIDR blocks
                          for a managed Rancher service's IP restrictions. When empty,
                          any IP is allowed (see below for nested schema)
                        items:
                          properties:
                            cidrBlock:
                              description: |-
                                (String) Allowed CIDR block (/subnet is optional, if unspecified then /32 will be used)
                                Allowed CIDR block (/subnet is optional, if unspecified then /32 will be used)
                              type: string
                            description:
                              description: |-
                                (String) Description of the allowed CIDR block
                                Description of the allowed CIDR block
                              type: string
                          type: object
                        type: array
                      name:
                        description: |-
                          (String) Name of the managed Rancher service
                          Name of the managed Rancher service
                        type: string
                      networking:
                        description: (Attributes) Networking properties of a managed
                          Rancher service (see below for nested schema)
                        properties:
                          egressCidrBlocks:
                            description: |-
                              (List of String) Specifies the CIDR ranges for egress IP addresses used by Rancher. Ensure these ranges are allowed in any IP restrictions for services that Rancher will access.
                              Specifies the CIDR ranges for egress IP addresses used by Rancher. Ensure these ranges are allowed in any IP restrictions for services that Rancher will access.
                            items:
                              type: string
                            type: array
                        type: object
                      plan:
                        description: |-
                          (String) Plan of the managed Rancher service. Available plans for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/plan
                          Plan of the managed Rancher service
                        type: string
                      region:
                        description: |-
                          (String) Region of the managed Rancher service
                          Region of the managed Rancher service
                        type: string
                      url:
                        description: |-
                          (String) URL of the managed Rancher service
                          URL of the managed Rancher service
                        type: string
                      usage:
                        description: (Attributes) Latest metrics regarding the usage
                          of the managed Rancher service (see below for nested schema)
                        properties:
                          datetime:
                            description: |-
                              (String) Date of the sample
                              Date of the sample
                            type: string
                          orchestratedVcpus:
                            description: |-
                              (Number) Total number of vCPUs orchestrated by the managed Rancher service through the downstream clusters
                              Total number of vCPUs orchestrated by the managed Rancher service through the downstream clusters
                            type: number
                        type: object
                      version:
                        description: |-
                          (String) Version of the managed Rancher service. Available versions for an existing managed Rancher can be retrieved using ovh_cloud_project_rancher_version datasource. Default is the latest version.
                          Version of the managed Rancher service
                        type: string
                    type: object
                  currentTasks:
                    description: (Attributes List) Asynchronous operations ongoing
                      on the managed Rancher service (see below for nested schema)
                    items:
                      properties:
                        id:
                          description: |-
                            (String) Unique identifier
                            Identifier of the current task
                          type: string
                        link:
                          description: |-
                            (String) Link to the task details
                            Link to the task details
                          type: string
                        status:
                          description: |-
                            (String) Current global status of the current task
                            Current global status of the current task
                          type: string
                        type:
                          description: |-
                            (String) Type of the current task
                            Type of the current task
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) Unique identifier
                    type: string
                  projectId:
                    description: |-
                      (String) Project ID
                      Project ID
                    type: string
                  rancherId:
                    description: |-
                      (String) Unique identifier
                      Rancher ID
                    type: string
                  resourceStatus:
                    description: |-
                      (String) Reflects the readiness of the managed Rancher service. A new target specification request will be accepted only in READY status
                      Reflects the readiness of the managed Rancher service. A new target specification request will be accepted only in `READY` status
                    type: string
                  targetSpec:
                    description: (Attributes) Target specification for the managed
                      Rancher service (see below for nested schema)
                    properties:
                      iamAuthEnabled:
                        description: |-
                          (Bool) Allows Rancher to use identities managed by OVHcloud IAM (Identity and Access Management) to control access
                          Allows Rancher to use identities managed by OVHcloud IAM (Identity and Access Management) to control access
                        type: boolean
                      ipRestrictions:
                        description: (Attributes List) List of allowed CIDR blocks
                          for a managed Rancher service's IP restrictions. When empty,
                          any IP is allowed (see below for nested schema)
                        items:
                          properties:
                            cidrBlock:
                              description: |-
                                (String) Allowed CIDR block (/subnet is optional, if unspecified then /32 will be used)
                                Allowed CIDR block (/subnet is optional, if unspecified then /32 will be used)
                              type: string
                            description:
                              description: |-
                                (String) Description of the allowed CIDR block
                                Description of the allowed CIDR block
                              type: string
                          type: object
                        type: array
                      name:
                        description: |-
                          (String) Name of the managed Rancher service
                          Name of the managed Rancher service
                        type: string
                      plan:
                        description: |-
                          (String) Plan of the managed Rancher service. Available plans for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/plan
                          Plan of the managed Rancher service. Available plans for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/plan
                        type: string
                      version:
                        description: |-
                          (String) Version of the managed Rancher service. Available versions for an existing managed Rancher can be retrieved using ovh_cloud_project_rancher_version datasource. Default is the latest version.
                          Version of the managed Rancher service. Available versions for an existing managed Rancher can be retrieved using GET /rancher/rancherID/capabilities/version. Default is the latest version.
                        type: string
                    type: object
                  updatedAt:
                    description: |-
                      (String) Date of the last managed Rancher service update
                      Date of the last managed Rancher service update
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
